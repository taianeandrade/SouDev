let n = 100;

while (n >= 1) { 
  document.write("<br>" + n);

    // n = n + 100;
    // n += 1;
    n--; 
}

document.write("<hr>");

let numero = 1;

while (numero <= 100) { 
    if (numero % 2 == 0)
    //se entrar nesse IF é porque é par
  document.write("<br>" + numero);
  numero++; 
}














